*** Variables ***
# to help catch hard-coded paths
${URL PREFIX}       /@est/
${FIXTURES}         ${CURDIR}${/}..${/}fixtures
${NBSERVER CONF}    jupyter_notebook_config.json
${NEXT LAB}         ${0}
